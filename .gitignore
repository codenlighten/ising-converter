# Rust
/target/
# Cargo.lock is tracked: this crate builds a cdylib (application), so a
# committed lockfile gives reproducible builds.

# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
.pytest_cache/
.venv/
venv/
dist/
build/
*.whl
